disposition: ship

Fix-round 1 verdict scope: M1–M3 are all resolved. The reviewer scored these three fixes and their visible side effects; other matrix rows below retain the original full review's findings.

Inputs not independently observed: reference-video playback and interaction/motion execution. Root reports all 22 workbench cases passing, including compact controls and both motion-preference recovery cases; these are root-observed functional results. No approved comp applies to this confirmed code-first revision; the previous DESIGN.md is superseded and awaiting documentation.

## persistence

Pass for this review's prerequisites. PRODUCT.md and the current workbench direction exist. All 30 regenerated originals named in the updated `dist/evidence/compact-manifest.json` were individually reopened for this verdict: each is valid, populated and consistent with its named state and dimensions. Coverage includes 1440×1000, 1198×958, 1401×958 and 390×844, both appearance themes, the dropdowns, inspectors, token types, creation, List, Themes, Manage, Files, Library and sticky navigation. The supplied detector result is `[]`; it was not rerun. All three owner-supplied still references were opened in the original review. The latest explicit direction governs; the rejected earlier visual world does not. Final design documentation remains owed after this fix verdict.

## fidelity

| Element / promise | State | Evidence |
| --- | --- | --- |
| TYPE — Geist primary, compact hierarchy | match | Latin chrome has a consistent compact face and clear label/title steps; source locally loads Geist first and SUIT as fallback. Korean copy fits at both actual desktop widths and 390px. Authored typography tokens correctly retain their own values. |
| GROUND — achromatic light and charcoal | match | Both themes read neutral. Original-review samples were light canvas RGB 237/237/237, separators 229/229/229, and inspector field 248/248/248; the final light separator correction is scored below. Dark chrome is charcoal; the purple, blue and other specimen colors belong to authored tokens. |
| MATERIAL — glass, blur and restrained edge light across chrome | match — M1 resolved | The regenerated dark dropdown visibly diffuses purple swatches beneath its options while preserving readable labels. Sidebar, inspector, header and footer now share visible neutral light falloff and translucent layering. Light Domains, system and mobile captures show discernible pane depth and edge light rather than the former uniform white/gray fields. |
| FIRST VIEWPORT — compact shell and workspace navigation | match | 52px global header, compact 36px workspace choices, small icon controls and reduced content insets are visible. Domain materials lead without the former large hero. |
| Aligned panel bars | match — M3 resolved | Color, spacing and creation context headers now occupy the same 44px bar and end at Foundation navigation's y=96 baseline. The previous approximately 15px stagger is gone. Component canvas/selection alignment remains intact. |
| Darker borders and clear light-theme structure | match — M2 resolved | Light Domains and system captures now show continuous, readable panel boundaries and section rules. Mobile header/context dividers are also clearly separated. Filled view controls and specimen selection retain their existing treatment without added outlines. |
| Filled Specimens/List controls and specimen selection | match | The view switch uses a filled active segment. Selected specimen cards use one neutral filled wrapper; its narrow exposed edge is that same fill, not a competing selection outline. Specimen cards have no persistent bulk checkbox. |
| Square color tiles, family names and local labels | match | `color.brand` heads square swatches labeled `50`, `100`, `200`, etc. Full names remain available in source and accessible labels. |
| STORY — contextual creation and inspection | match | Family add buttons lead to a populated `color.brand.` creation context. Token-specific editors disclose color, dimensions, shadow and typography controls. This establishes visual flow only; transaction, keyboard and recovery behavior belong to functional verification. |
| Compact component inspector and icon canvas controls | match | `compact-component-inspector.png` removes the large identity header, starts from Design/Source and compact property sections, and uses icon choices for Web/Mobile, Edit/Try and preview theme. |
| Sticky Foundation navigation | match | `compact-sticky-tokens.png` shows the Foundation tabs remaining at the top while token content is scrolled. The still does not establish scrolling or motion quality. |
| Responsive composition | adaptation | Per PRODUCT.md's desktop-first requirement, 390px uses navigation/workspace/inspector tabs and a two-column specimen grid. Captured content stays within the viewport and preserves readable labels. |
| FORM and requested removals | match | The current brief records the owner's specific superseding direction and confirmed code-first/both-theme choice. The Axiom UI sidebar footer and component identity heading are absent. The supplied reference stills support compact controls and neutral grounds; no video-viewing claim is made here. |

## ceiling

Reached for the three named corrections: the glass finish is visible across chrome, the light separators establish structure, and the token bars align. No material visual regression from this fix batch was observed in the required captures. Motion quality cannot be graded from these stills.

## material_fixes

1. **M1 · resolved:** The menu visibly frosts its underlying content, and panel chrome shares a coherent glass finish across dark and light captures with readable labels.
2. **M2 · resolved:** Stronger light rules separate sections and panel boundaries while the filled controls and specimen selections remain intact.
3. **M3 · resolved:** Token and creation headers now align with Foundation's 44px bar; component bars remain aligned.

Remaining: clear for M1–M3. This ship disposition covers the scored fixes. Functional verification, motion execution and final documentation retain their separate scopes.

## keep

Keep the compact 36px navigation, Geist hierarchy, achromatic grounds, fluorescent action accent, authored token colors, square specimens, family/local naming, contextual creation, opt-in specimen bulk selection, custom menus, sticky tabs and preserved source/review/recovery semantics.
