# Disposition — ship

Finish review, 2026-09-15. **Ship the bounded component editing increment on visual finish.** The final dark/light composition captures confirm the compact instance Boolean checkbox and blank empty containers. No material visual defect remains in the reviewed states. No additional redesign, recapture or polishing is indicated. Repository-wide execution gates remain separate.

# Scope and evidence

The authority is the code-first Operate direction in `apps/studio/PRODUCT.md`, `apps/studio/DESIGN.md` and `apps/studio/.impeccable/surfaces/workbench.md`, with the Impeccable craft floor. This reviewer is independent of the element/instance, behavior and library implementations. Policy implementation and its focused browser verification were performed by this reviewer, so policy observations are a self-review, not an independent certification.

Opened rasters under `dist/evidence`: `composition-{dark,light}.png` at 1600 × 1080; `policy-editor-desktop-dark.png`, `policy-required-desktop-{dark,light}.png` at 1440 × 1080; `policy-required-narrow-{dark,light}.png` at 390 × 844; and `library-baseline-{1440,390}-{dark,light}.png`. The library images are a seven-component specimen harness, not the complete Studio Library page. The final composition files, refreshed at 12:48:59 KST on 2026-09-15 from the integrated workbench run, replace the earlier captures. Both were opened together for the final confirmation pass.

Independently read SHA-256 hashes for these replacement files: dark `B743A7C390A8971E778BA6AE201E5F8C36EAD301D0EB9E248FA2582CB945C335`; light `E76FFD24BF3A4A61256B36CF6611AB860AEA64D33F04055A3FBA3B34958B685B`.

Supporting records: `composition-focused.json` reports two Chromium cases passing in 86,763 ms; `behavior-focused.json` reports one passing in 22,640 ms; `policy-focused.json` records this reviewer's two passing cases in 25,442 ms; `library-baseline-visual.json` reports four width/theme states without horizontal overflow. Relevant element-selection, element-authoring, instance-inspector, catalog-preview, component-inspector, policy-panel and behavior-editor source was inspected. Repository-wide gates and native execution are the implementation owner's separate evidence.

# Material findings

1. **Instance Boolean control — resolved.** The first composition capture showed the checked override stretched into a long green bar by the generic first-child flex rule. Current CSS overrides that rule for the checkbox with `flex: 0 0 auto` and 16 × 16 px dimensions. The final dark and light captures visibly confirm a compact square checkbox aligned with its label and Reset action.
2. **Element affordance consistency — corrected.** Selection controls now override consumer button minimum dimensions, use the defined Studio separator and font roles, and pair the inline text editor's foreground and background from the same theme. Empty Box and Frame containers no longer insert their layer names as visible authored content. The final dark and light captures show those surfaces blank while the corresponding names remain in the Elements tree. The still-image set does not establish every drag or inline editing state.
3. **Behavior code typography — source corrected.** Undefined `--font-mono` references in the behavior textarea and state output were replaced with the existing `--font-code` role. There is no dedicated behavior raster in this review, so this finding is source-based.
4. **The earlier apparent light-preview blank was not a demonstrated product defect.** Direct pixel inspection of the superseded captures found 3,005 blue Button pixels in both themes and identical dark-pixel counts in the frame-label crop. The underlying files contained the content that appeared absent in this reviewer's image display. `component-editing-pixel-check.json` records those earlier crop bounds, counts and hashes; it does not describe the replacement images. The final light capture visibly paints the Button, checkbox label and authored text. The earlier diagnostic establishes pixel presence only; it is not a contrast or accessibility result.

# Craft and behavior limits

The reviewed Studio surfaces retain achromatic glass planes, restrained green actions, Geist hierarchy and compact controls. Policy rules use plain labels, visible severity and rationale, local edit/delete actions and component → element → category locations. The final policy captures show readable source locations and aligned controls at desktop and narrow widths. Primitive, semantic and component policy constraints are operational, with required delivery failures separated from editable drafts. This does not implement arbitrary predicates, scripting, approval exceptions or every Foundation policy obligation.

Box, Frame and Text creation and the colocated Elements, Content area and Instances controls present a coherent authoring model. The composition record exercises pointer movement/resizing at canvas zoom, inline text save/reload, reviewed history, real checkbox insertion, override/source separation and explicit source refresh. It does not establish a full Figma/Framer feature set. No narrow element/instance raster is available; responsive source was inspected without claiming that unobserved state passed visual review.

The behavior record covers local state, typed trigger/condition/actions, request trace, repeated simulation, source preservation, incomplete-input recovery, review, Undo and reload. This is bounded Chromium behavior evidence; no dedicated behavior screenshot, operating-system IME, assistive-technology or native execution was reviewed here. Policies' narrow captures show the top of a scrollable rule list rather than every form field or footer. No independent computed contrast audit was performed.

The seven library baselines are recognizably distinct: Button, Card, Switch, Input, Checkbox, Select and Tabs. Selected tabs use a single fill. Consumer previews may intentionally contain native inputs/selects; they are distinct from Studio's own custom form controls. The specimen harness's surrounding cards are test layout, not evidence of nested cards in the shipped Library UI. These captures do not certify the entire catalog or every target runtime.

# Final verdict

**Ship on visual finish.** The final composition evidence closes the checkbox finding and confirms empty-container parity with authored output in both Studio themes. No further visual fix or confirmation pass is indicated for the reviewed increment. This verdict does not establish full Foundation completion, native readiness or exhaustive catalog verification. Repository gates and publishing remain separate obligations.
