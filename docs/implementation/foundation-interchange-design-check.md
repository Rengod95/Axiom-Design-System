# Foundation interchange design agreement check

Date: 2026-09-14. Scope: the Foundation interchange and live-expression increment authorized by [ADR-0017](../adr/0017-foundation-interchange-and-live-expressions.md), described in the [continuation](foundation-interchange-continuation.md).

**Disposition: source and design agreement confirmed for this ordinary extension.** The established Operate system remains SUIT, neutral surfaces, restrained violet actions and selection, and separate light/dark Studio themes. The owner's code-first and both-theme decisions are already recorded in the [workbench brief](../../apps/studio/.impeccable/surfaces/workbench.md). No new visual world or token scale was introduced. This check preserves [PRODUCT.md](../../apps/studio/PRODUCT.md), [DESIGN.md](../../apps/studio/DESIGN.md), and the [v2 design sidecar](../../apps/studio/.impeccable/design.json).

## Sources and method

The documenter read those design artifacts, the Impeccable `reference/document.md` playbook, and the working-tree implementation in [Foundation Files](../../apps/studio/src/foundation-exchange-panel.tsx), [expression editing](../../apps/studio/src/foundation-expression-editor.tsx), [token inspection](../../apps/studio/src/foundation-inspector.tsx), [Foundation navigation](../../apps/studio/src/foundation-panel.tsx), [relationships](../../apps/studio/src/token-relationships.tsx), [Foundation CSS](../../apps/studio/src/foundation-workspace.css), and [shared controls](../../apps/studio/src/ui.tsx). The comparison also follows the actual stylesheet order in [main.tsx](../../apps/studio/src/main.tsx): `styles.css`, `ui-system.css`, then `foundation-workspace.css`. Specificity and markup ancestry remain significant after that ordering.

This is a static source/documentation comparison. The existing detector and finish-review records were read; neither the detector nor a browser capture was rerun by this documenter. The shared session had already loaded Impeccable context once.

## Agreement checked

| Area | Implementation and documented agreement |
| --- | --- |
| Palette | A direct comparison found **50 of 50** frontmatter light/dark color values equal to the corresponding runtime values, including the primary-button foreground literals named `on-accent` in documentation. The new CSS references `surface`, `ink`, `ink-secondary`, `line`, `line-strong` and existing accent/control roles; it defines no new palette. Errors retain negative text and invalid boundaries. Studio theme selection remains independent of authored Foundation contexts. |
| Typography | The runtime SUIT UI stack and monospace source stack match the documented families. Shared sizes remain 11/12/13/16/24px, with medium/strong weights 550/650 and label/body leading 1.4/1.6. Files uses the existing 16px section title, 13px subsection/count/body sizes, 12px preview rows/actions, and 11px hints. It retains the documented inherited weights rather than claiming a global typography reset. |
| Spacing and shape | New rules use existing 8/12/16/20/24/32px steps for groups, rows, headings and workspace spacing. Files has 24px outer padding, a 32px column gap, 16px source/preview field gaps, 12px token-row padding and 1px structural separators. Bindings use 12px inner gaps and 16px group separation. The native file-selector button uses the 7px control radius and existing stronger neutral outline. No new structural shadow or panel radius appears. |
| Layout | Import uses `minmax(0, 1.15fr)` and `minmax(240px, 1fr)` columns; at the existing 1200px breakpoint it becomes one column with a separated preview. Export stays within 65ch. Names wrap and flex children use `min-width: 0`. Files retains 24px padding on narrow screens; the older 1100px padding reduction targets domain/starter/relationship panels only. The established 720px single-panel shell remains authoritative. |
| Controls | Conflict, Resolver context and export choices reuse `Button` inside the existing wrapping choice group, with `aria-pressed` and the accent selected treatment. Apply/download use primary actions; optional prefix/source sections use native disclosures. Source/property bindings reuse labeled native selects; lifecycle reuses `Section`, the labeled checkbox row and multiline `TextInput`. Shared buttons keep the documented 28/34/40px minimum-size model and its narrow-screen exceptions. The file-selector pseudo-element reuses field/secondary visual roles; it is not claimed to implement the full React Button API. |
| State and recovery | The source contains explicit empty-source, disabled, file-read, invalid-preview, applied-preview, invalid-expression, reset and download-error states. Real control labels, native disabled semantics, pressed choices, alert/status text, the shared focus ring and the existing reduced-motion override remain. Files staging stays mounted while hidden and separate from project drafts. Export copy explains selected-theme output, unsaved preview values, and whole-project preservation. This check does not independently execute those flows. |
| Relationships | Property/composite dependency rows retain the existing token-node, usage-link, indentation, caption metadata, selection and quiet-hover styles. The sidecar's Foundation Usage Link still represents the same visual primitive; source-controlled navigation and graph limits remain runtime behavior. |

## Effective cascade and sidecar comparison

The local declarations do not all become computed styles. The import JSON textarea is inside `Field`, so the more-specific shared app-field selector supplies **13px / 1.4**, a 76px minimum and 8px vertical padding; its monospace family remains. Native source file inputs also take shared **13px** field text over the local 12px declaration. The optional expression JSON textarea is outside `Field`, so it keeps **11px / 1.6** monospace, the base 72px minimum and 7px by 9px padding. Its source/property selects and lifecycle guidance field use the ordinary shared field styling. These differences agree with DESIGN.md's scoped field/code roles and its explicit unmatched-textarea exception.

The token-table deprecation annotation declares the caption role, but the incumbent `.token-name-cell small` selector wins at **10px**; medium weight and secondary ink still apply. Preview-list `small` text retains the global 12px size. Supplied Resolver filenames use plain spans in the wrapping group and inherit the documented 14px base-body size; `value-badge` has no dedicated CSS rule and is not the sidecar's 10px Status Badge primitive. These are actual cascade results, not assertions that every new label uses a shared caption token.

The sidecar remains an extension of normative frontmatter: its palette metadata, typography-role descriptions, control sizing, spacing applications, breakpoints, source order and representative Button/Field/Tab/Usage snippets agree with the reused source primitives. Its static Inspector Tabs snippet correctly captures horizontal overflow, 20px gap, 12px labels, 600 selected weight and the 2px ink underline. Static snippets do not execute React selection or scrolling behavior. No primitive change requires regeneration of DESIGN.md or the sidecar in this scoped pass.

## Selected-tab correction and evidence boundary

The reviewed [TabBar source](../../apps/studio/src/ui.tsx) now reveals the active tab on selection/label changes and observes both strip and selected-tab size with `ResizeObserver`. It adjusts only the strip's `scrollLeft`, with a 4px visibility inset, and ignores a zero-width hidden strip until it becomes measurable. Arrow Left/Right, Home/End, roving `tabIndex` and `aria-selected` remain; keyboard focus uses `focus({ preventScroll: true })`. The observer is disconnected during effect cleanup. This changes behavior without changing the tab typography, spacing, palette or underline.

At this documenter's handoff, **the same finish reviewer returned “ship — R1 resolved”** in the durable [interchange finish review](foundation-interchange-finish-review.md). Its material correction was mobile active-Files-tab clipping. The documenter also read the final `dist/evidence/workbench.json`: status `PASSED`, 18 recorded cases, including `selectedTabVisibleOnResizeAndKeys` and `tabKeysPreserveParentScroll`. The corresponding [browser assertions](../../scripts/workbench-completion-cases.mjs) check selected bounds after desktop-to-390px resize and Workspace reveal, Home/End/Arrow navigation, unchanged ancestor vertical scroll, and no document overflow in the mobile light/dark flow. This is recorded browser evidence, not an independent rerun by the documenter.

The captures present at the initial documenter read showed the pre-R1 state and were not used to certify the correction. The finish reviewer subsequently opened all nine refreshed captures and confirmed the complete Files label and underline in both 390px themes. That bounded fix-list verdict closes R1; this source agreement check does not add a separate visual certification or reopen the whole surface.

## Preserved drift and limits

- The once-run `dist/evidence/interop-detector.json` has zero primary findings and one advisory for the 18px authored typography specimen in `foundation-workspace.css`. The same rule exists in `HEAD` before this increment. It remains outside the documented shared UI size ladder and is recorded without changing source, tokens or detector settings.
- Existing documentation already records residual root/headline sizes, 10px token metadata, 600 selected-tab weight, compact target exceptions and unmatched legacy spacing. The sidecar also records that the Axiom UI spacing specimen omits the declared 20px and 40px steps. Those pre-existing discrepancies were not normalized here.
- DESIGN.md and the sidecar retain their earlier completion-review evidence, including 343 tests and 16 flows. Those references identify the previous correction; they are not evidence for this increment. Current interchange execution belongs to the continuation evidence and finish-review records. The additive workbench surface note carries this increment's interaction contract without rewriting that history.

No all-119-requirement, complete DTCG/Resolver, physical-device, assistive-technology or native-execution conclusion follows from this check. No product code or token values were edited.
