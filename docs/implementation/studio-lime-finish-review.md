disposition: ship

Verdict scope: this ship verdict covers the four scored fixes, not the whole surface. All 19 recaptures were reopened at their original paths and match the refreshed SHA256 manifest. Static screenshots cannot establish temporal visual approval, and List rendering was not captured. No browser or detector ran; no new feature or surface hunt was performed.

## persistence

Resolved — FORM now contains direction seed `0b6cfbbd` and Operate slot 7. `docs/implementation/studio-lime-redesign.md` also has a clearly labeled execution note with successful API pool `c3b204a1eed6`; it explicitly states that no raw log was saved. The note does not pretend to be raw output. PRODUCT.md and the code-first decision remain the established context; final DESIGN.md documentation follows this verdict.

Evidence pass — all required recaptures are present, nonblank, and appropriate to their named states: domains-dark, atlas-dark, color-inspector, spacing, radius, shadow, typography, motion, opacity, sizing, component-inspector, system-dark, system-controls, system-motion, system-light, domains-light, user-1312, mobile, and mobile-light. Desktop, the 1312px Korean viewport, and both 390px mobile themes remain intact.

## fidelity

| Scored fix | Verdict | Evidence |
| --- | --- | --- |
| 1. Foundation inspector hierarchy | resolved | In `lime-shadow.png`, X/Y, Blur/Spread, Inset, the compact Color/Hex/Alpha row, and Apply to preview are visible together in the initial 1440x1000 inspector view. In `lime-color-inspector.png`, picker, Hex, and Alpha share the primary row; Color space and channels is progressively disclosed. This answers the practical editing hierarchy rather than merely moving labels. |
| 2. Composite specimen captions | resolved | `lime-shadow.png` now shows readable X/Y, Blur, and Spread recipes under each shadow specimen, including visible differences between lg, md, and sm. Captions wrap cleanly instead of showing truncated JSON. `specimenSummary` is wired to the atlas; precise source remains outside this display summary. |
| 3. Direction-seed persistence | resolved | FORM contains the corroborated seed, and the execution note records its provenance and limitations explicitly. |
| 4. Shared neutral control role | resolved | The atlas, sidebar search, Axiom UI control gallery, and light/mobile theme captures now use the same neutral outline family. The source values are `#7a826f` in light and `#747f66` in dark. Calculated border-to-surface contrast is approximately 3.89:1 light and 4.22:1 dark; against the sidebar it is approximately 3.62:1 and 3.98:1. Existing focus roles remain unchanged. |

Regressions introduced by this batch: none observed in the supplied recaptures. The prior TYPE, MATERIAL, GROUND, navigation, and responsive judgments were not reopened as a new hunt.

## ceiling

Reached for the scored fixes. The Foundation inspector now uses the reference's practical grouping, the shadow sheet supports comparison, and the shared control palette belongs to the new neutral/lime world.

## material_fixes

Clear. All four prior material fixes are resolved; no partial or unresolved finding remains in this fix batch. Functional test completion and final documentation remain the parent's separate finish obligations and are not inferred from screenshots.

## keep

Keep the lime selection signal, SUIT hierarchy, actual material specimens, preserved project colors, both-theme composition, newly compact Foundation controls, readable specimen recipes, restrained floating glass, and reviewed-change boundary.
