# ADR-0008 — Foundation review and reproducible documentation checks

Status: ACCEPTED for documentation maintenance only

Date: 2026-09-13. Owner: product owner; executor: Codex.

The owner requested an exhaustive review of all 56 documents, correction of omissions/overlap/quality gaps, automatic merge of PR #25 when no material issues remain, then product implementation. This is conditional authorization. It does not record a successful review before evidence exists.

The review checks each manifest document's ownership, completion criteria, concrete normal/failure flows, structured annexes and approved decisions. A later technology selection with a bounded evidence gate is not a missing product requirement. Material inconsistencies must be corrected before the merge.

ADR-0007's documentation phase remains active while the audit runs. Add only `scripts/verify-foundation.py` to its maintenance allowlist and invoke it in Quality Gate alongside the unchanged preservation checks. This exception is scoped to document inventories/hashes, decision ownership, dependency DAG, local anchors, strict JSON and prose/catalog projections. Its negative fixtures must reject missing bodies, drift, bad anchors, cycles, duplicate JSON keys and missing operation projections.

The initial 2026-09-12 QA is historical evidence at commit `08a2c031f377e4d2da4bb0851a7c649392b6fea3`; current QA is regenerated by the checked-in verifier after edits. Human/agent semantic findings remain a separate 56-document audit. Neither report is product behavior evidence.

After all material findings are resolved, record the owner's conditional approval as fulfilled, pin the reviewed commit for PR merge, and accept a separate implementation-bootstrap ADR before changing the product phase guard. This ADR does not authorize an arbitrary source layout or claim that Studio/native targets are implemented.

Review condition: revisit when document contracts or maintenance checks change. The product implementation profile must preserve these checks and snapshot integrity while defining its own positive/negative runtime suite.

References: [ADR-0007](0007-git-reference-and-documentation-phase.md), [baseline review](../foundation/baseline-review.md), [development sequence](../foundation/operations/development-and-maintenance.md).
