{
  "name": "@axiom/recipe-kernel",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "check:types": "tsc -b && tsc -p tsconfig.type-tests.json --pretty false"
  },
  "devDependencies": {
    "@axiom/condition-registry": "workspace:*",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
