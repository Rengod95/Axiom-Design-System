{
  "name": "axiom-design-system",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b",
    "check": "pnpm check:boundaries && pnpm spec:check && pnpm generate:check && tsc -b --pretty false",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "clean": "tsc -b --clean",
    "generate": "node --import tsx packages/tokens/scripts/generate.ts && tsc -b packages/tokens packages/appearance-schema packages/recipes packages/recipe-engine && node --import tsx packages/adapter-tailwind/scripts/generate.ts",
    "generate:check": "pnpm generate && git diff --exit-code -- packages/tokens/src/generated packages/adapter-tailwind/src/generated",
    "spec:check": "node --import tsx packages/spec-tooling/src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
